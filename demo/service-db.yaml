apiVersion: v1
kind: Service
metadata:
  name: db
spec:
  selector:
    app: db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      # если кластер или ничего - то не доступно извне, если loadbalancer - доступно
  type: LoadBalancer
